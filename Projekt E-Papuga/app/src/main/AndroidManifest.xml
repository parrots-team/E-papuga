<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.epapuga">
    <!-- Camera Storage permissions for image pick -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Epapuga">
        <activity android:name=".profil.RecordDetailActivity"></activity>
        <activity android:name=".profil.AddUpdateRecordActivity" />
        <activity android:name=".profil.Profil" />
        <activity
            android:name=".Wikipedia.WikiActivity2"
            android:label="Gatunek"
            android:parentActivityName=".Wikipedia.WikiActivity" />
        <activity
            android:name=".Wikipedia.WikiActivity"
            android:label="Papugedia" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Kalendarz.CalendarActivity" />
        <activity android:name=".LoginRegister.RejestracjaActivity" />
        <activity android:name=".LoginRegister.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Image cropper activity -->
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Theme.Epapuga" />
    </application>

</manifest>