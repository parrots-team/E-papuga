<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.epapuga">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Epapuga">
        <activity
            android:name=".UpdateActivity"
            android:parentActivityName=".ProfilActivity" />
        <activity
            android:name=".AddActivity"
            android:label="Dodaj Papugę"
            android:parentActivityName=".ProfilActivity" />
        <activity
            android:name=".WikiActivity2"
            android:label="Gatunek"
            android:parentActivityName=".WikiActivity" />
        <activity
            android:name=".WikiActivity"
            android:label="Papugedia" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ProfilActivity"
            android:label="Papugi" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".RejestracjaActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>